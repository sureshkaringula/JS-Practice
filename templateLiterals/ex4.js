let person = {name:"Suresh",age:27};

console.log(`User ${person.name} is ${person.age} years old`);


let fruits = ["apple", "banana", "mango"];

fruits.forEach(fruit => {
  console.log(`I like ${fruit}`);
});
